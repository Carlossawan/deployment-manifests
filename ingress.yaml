apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-ingress
  namespace: testcarlos
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: "192.168.154.101.nip.io"
    http:
      paths:
      - path: /user
        pathType: Prefix
        backend:
          service:
            name: nginx-service
            port:
              number: 80
